<!DOCTYPE html>
<html lang="en">
<head>
 <%-include("./partials/head")  %>
</head>
<body>
 <%-include("./partials/nav")  %>  
 <%= JSON.stringify(user) %>  
<!-- display the full blog ehre 
  -->
<div class="cont mt-4px">

    <h1><%= blog.title %></h1>
    <img src="<%= blog.coverimage %>" width="500px" alt="blogimage">
<pre><%= blog.blogbody %></pre>
</div>
<div class="cont mt-4px">
<h2><%= blog.author.name %></h2>
<div class="d-flex align-items-center gap-2">
  <img src="<%=blog.author.profileimg  %>" width="50" height="50" class="rounded-circle">
  
</div>

</div> 
 <% if(locals.user) {%>
<div class="cont mt-4">
    <form action="/blog/comments/<%= blog._id %>" method="post">
 <div class="mt-3">
      <label for="examplename" class="form-label">COMMENT</label>
      <input type="text" class="form-control" id="examplename" name="content">
  <button type="submit">ADD</button>
</div>
  </form> 
</div>
<!-- blog comemenst how ?  -->
 <% } %>
 <h1>COMMENTS (<%= comment.length %>)</h1>
 <div class="cont mt-4">
 <% if (typeof comment !== 'undefined' &&  comment.length> 0) { %>
<!-- <h2><%=  %></h2> -->
<%    comment.forEach((com)=>{ %> 
 <div class="cont mt-4px background-color" >
<div class="d-flex align-items-center gap-2">
  <img src="<%= com.useri.profileimg %>" width="30" height="30" class="rounded-circle">
  <h6 class="mb-0"><%= com.useri.name %></h6>
</div>

<pre style="font-size: 20px;"><%= com.content %></pre>
 </div>
<% }) %>
    <% } %>
 </div>

 </body>
 <%-include("./partials/scripts")  %>

</html>